App/build
App/Pods
App/output
App/App/public
DerivedData
xcuserdata

# Cordova plugins for Capacitor
capacitor-cordova-ios-plugins

# Generated Config files
# capacitor.config.json is intentionally tracked: it carries packageClassList
# (the native plugin registration list). Leaving it generated-only meant a
# build without a preceding `cap sync` shipped without PushNotificationsPlugin.
App/App/config.xml
